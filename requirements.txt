numpy==2.5.2
openpyxl>=3.1
pandas==3.0.5
matplotlib>=3.10
scipy==1.18.1
h5py==3.16.0
PyYAML==6.0.3
packaging==26.3
traitlets==5.16.1
pillow==12.3.0
pyqtgraph==0.14.0

# Pip-only packages (from environment.yml pip subsection)
Markdown==3.10.3
python_markdown_math==0.9
pytest==9.1.1
setuptools==82.0.1

 # NOTE: This `requirements.txt` is generated from `environment.yml`.
 # Keep PyQt packages managed by conda (do NOT list PySide6 here).
 # If you add/remove pinned versions in `environment.yml`, run the
 # sync workflow or `tools/sync_env_from_requirements.py` to update this file.